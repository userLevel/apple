#pragma keep "globals"/* --------------------------------------------------------------- *//*                                                                 *//*   Global data for MiniWord                                      *//*                                                                 *//* --------------------------------------------------------------- */#include "globals.h"int        done;                 /* true if user is ready to exit MiniWord    */int        myID;                 /* MiniWord's user ID                        */Handle     buffer;               /* buffer to hold text returned by TEGetText */long       bufferSize;           /* size of this buffer                       */TEInfoRec  textInfo;             /* gives # lines, # chars in a document      */char okTitle [] = "\pOK";                             /* common button titles */char cancelTitle [] = "\pCancel";/* Window tracking information -- We're allowing only 4 windows *//* to be opened on the desktop.                                 */char             *wTitle;                   /* pointer to window's title      */long             wRefCon;                   /* index into window arrays       */enum windowType  windowOpen   [4];          /* array of open window flags     */GrafPortPtr      windowPtr    [4];          /* array of pointers to grafPorts */CtlRecHndl       textEdHandle [4];          /* array of editText ctl handles  */Handle           pathHandle   [4];          /* array of pathname handles,     */                                            /*   returned by SFO              */