ECHO [-D] [-T] stringThe string is written to standard out.  Variables contained in the string areexpanded before writing.By default, the ECHO command replaces tab characters with a single space.  Ifthe -T flag is used, tabs are not replaced with spaces.Parameters:     -D         Don't write a carriage return after writing the string.     -T         Don't expand tabs.     string     String to print.