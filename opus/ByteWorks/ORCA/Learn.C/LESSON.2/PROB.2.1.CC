/* Write out five Fibonacci numbers */#include <stdio.h>int last;                               /* last number */int current;                            /* current number */int next;                               /* new number */void main(void){last = 0;current = 1;next = last+current;printf("%d\n", next);last = current;current = next;next = last+current;printf("%d\n", next);last = current;current = next;next = last+current;printf("%d\n", next);last = current;current = next;next = last+current;printf("%d\n", next);last = current;current = next;next = last+current;printf("%d\n", next);last = current;current = next;}