      REM Insert      REM      REM This program tests the function INSERT$, which inserts one      REM string into another.  It will ask for two strings and a position,      REM and will insert the second string into the first at the given      REM position.  It continues doing this until a string of length zero      REM is entered for both strings.  To get a string of length zero,      REM press the RETURN key without typing any other character.      DIM S1 AS STRING :! string to insert into      DIM S2 AS STRING :! string to insert      DIM POSITION AS INTEGER :! position to insert the string      ! Loop until there are no input strings.      DO        ! Get the strings and position.        LINE INPUT "String to insert into: ";S1        LINE INPUT "String to insert     : ";S2        INPUT "Position to insert   : ";POSITION        ! Insert the string and print the result.        PRINT "Result               : ";INSERT$(S1, S2, POSITION)        PRINT      LOOP WHILE LEN (S1) + LEN (S2) <> 0      END      !----------------------------------------------------------      !      ! Insert$ - Insert one string into another      !      ! Parameters:      !    s1 - string to insert into      !    s2 - string to insert      !    position - position to insert before; use 0 or 1 to insert      !         before the first character, or LEN(S1) + 1 to      !         insert after the last character      !      ! Returns: First string with the second inserted at position      !      !----------------------------------------------------------      FUNCTION INSERT$(S1 AS STRING , S2 AS STRING , POSITION AS INTEGER ) AS STRING      IF POSITION < 2 THEN        INSERT$ = S2 + S1      ELSE IF POSITION > LEN (S1) THEN        INSERT$ = S1 + S2      ELSE        INSERT$ = LEFT$ (S1, POSITION - 1) + S2 + RIGHT$ (S1, LEN (S1) - POSITION + 1)      END IF      END FUNCTION