#pragma lint -1#include <orca.h>#include <stdio.h>#include <QuickDraw.h>int main (void){unsigned color;                         /* pen color */unsigned i;                             /* loop variable */startgraph(640);color = 0;for (i = 0; i < 640; ++i) {   SetSolidPenPat(color);   MoveTo(i, 0);   LineTo(i, 199);   color = color ^ 3;   }scanf("%*[^\n]");endgraph();}