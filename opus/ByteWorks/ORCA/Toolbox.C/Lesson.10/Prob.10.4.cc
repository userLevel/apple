/***************************************************************** Custom Font Demo****************************************************************/#pragma lint -1#include <stdio.h>#include <orca.h>#include <QuickDraw.h>#include <Font.h>#define bold 1                          /* bold bit flag */int main (void){FontID id;                              /* font ID */Rect r;                                 /* screen's GrafPort rect *//* set up the tools */startdesk(320);PenNormal();SetSolidPenPat(15);GetPortRect(&r);PaintRect(&r);/* set up the font */SetForeColor(7);SetTextMode(modeForeCopy);id.fidRec.famNum = times;id.fidRec.fontStyle = bold;id.fidRec.fontSize = 36;InstallFont(id, 0);/* draw the text */MoveTo(10, 50);DrawCString("Hello, world.");/* wait for a keypress */scanf("%*[^\n]");enddesk();}